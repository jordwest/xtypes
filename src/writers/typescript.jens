main =
  // This file was generated automatically

  ${messages}
--

docblock =
  /**
   * ${comment}
   */
--

namespace =
  ${doc}
  export namespace ${name} {
    ${content}
  }
--

struct =
  export type ${name} = {
      ${fields}
  }
--

tagged_union =
  export type ${name} =
    ${variants};
--

variant =
  | { t: '${name}' }
--

variant_with_content =
  | { t: '${name}', v: ${content} }
--
